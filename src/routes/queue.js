const express = require('express');
// const queueController = require('../controllers/queue');

const router = express.Router();

// router.get('/:shopId', queueController.getQueueByShopId);


module.exports = router;